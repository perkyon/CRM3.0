# 🔧 Исправление проблемы с деплоем

## Проблема
GitHub Actions падает с ошибкой при деплое на Vercel, а нативная интеграция Vercel не работает.

## Решение: Использовать нативную интеграцию Vercel

Я отключил деплой через GitHub Actions и настроил использование нативной интеграции Vercel.

### Что сделано:
1. ✅ Отключен `deploy-production` в GitHub Actions
2. ✅ Отключен `deploy-preview` в GitHub Actions
3. ✅ GitHub Actions теперь только тестирует и собирает проект

### Что нужно сделать СЕЙЧАС:

#### 1. Подключить репозиторий в Vercel

1. Зайдите в **Vercel Dashboard** → проект `crm-buro`
2. Перейдите в **Settings** → **Git**
3. Прокрутите **вверх** - должна быть секция с репозиторием
4. Если репозитория нет:
   - Нажмите **"Connect Git Repository"**
   - Выберите GitHub аккаунт
   - Найдите `perkyon/CRM3.0`
   - Нажмите **"Import"** или **"Connect"**

#### 2. Настроить параметры

После подключения убедитесь, что:
- ✅ **Production Branch**: `main`
- ✅ **Auto-deploy**: включен ✅
- ✅ **Ignored Build Step**: "Automatic"

#### 3. Добавить переменные окружения

**Settings** → **Environment Variables** → добавьте:
- `VITE_SUPABASE_URL` = `https://ykdtitukhsvsvnbnskit.supabase.co`
- `VITE_SUPABASE_ANON_KEY` = `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlrZHRpdHVraHN2c3ZuYm5za2l0Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjE2Nzg3MjAsImV4cCI6MjA3NzI1NDcyMH0.tjCfpEG30rxaCuu22EmV3kKGxH45FDMTJNuPknpsl7w`

#### 4. Проверить настройки сборки

**Settings** → **General**:
- **Build Command**: `npm run vercel-build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

#### 5. Сделать тестовый push

```bash
git add .github/workflows/ci.yml
git commit -m "fix: отключен GitHub Actions деплой, используется нативная интеграция Vercel"
git push origin main
```

#### 6. Проверить результат

1. GitHub Actions должен пройти успешно (только тесты и сборка)
2. В Vercel Dashboard → **Deployments** должен появиться новый деплой
3. Деплой должен завершиться успешно

## Почему это лучше?

1. **Нативная интеграция Vercel** работает надежнее, чем GitHub Actions
2. **Автоматические preview деплои** для PR без дополнительной настройки
3. **Меньше зависимостей** - не нужны secrets для Vercel в GitHub
4. **Проще отладка** - все логи в одном месте (Vercel Dashboard)

## Если деплой все еще не работает

1. Проверьте, что репозиторий подключен в **Settings** → **Git**
2. Проверьте webhook в GitHub → **Settings** → **Webhooks** (должен быть от Vercel)
3. Попробуйте отключить и снова подключить репозиторий в Vercel

