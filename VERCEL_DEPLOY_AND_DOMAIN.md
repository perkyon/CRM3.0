# 🚀 Деплой на Vercel и подключение домена

## Часть 1: Первый деплой на Vercel

### Вариант A: Деплой через Vercel CLI (быстрый способ)

1. **Установите Vercel CLI** (если еще не установлен):
```bash
npm i -g vercel
```

2. **Войдите в Vercel**:
```bash
vercel login
```
Откроется браузер для авторизации.

3. **Деплой проекта**:
```bash
# В корне проекта
cd /Users/ilafomincev/Desktop/CRM3.0

# Первый деплой (создаст проект)
vercel

# Деплой в production
vercel --prod
```

### Вариант B: Деплой через GitHub (рекомендуется для автоматических деплоев)

1. **Зайдите в Vercel Dashboard**: https://vercel.com/dashboard

2. **Нажмите "Add New..." → "Project"**

3. **Импортируйте репозиторий**:
   - Выберите GitHub аккаунт
   - Найдите репозиторий `CRM3.0` (или `perkyon/CRM3.0`)
   - Нажмите **"Import"**

4. **Настройте проект**:
   - **Framework Preset**: Vite (или оставьте Auto-detect)
   - **Root Directory**: `./` (оставить как есть)
   - **Build Command**: `npm run vercel-build` (уже указано в vercel.json)
   - **Output Directory**: `dist` (уже указано в vercel.json)
   - **Install Command**: `npm install` (по умолчанию)

5. **Нажмите "Deploy"**

### Настройка переменных окружения в Vercel

После первого деплоя нужно добавить переменные окружения:

1. В Vercel Dashboard → проект → **Settings** → **Environment Variables**

2. Добавьте следующие переменные (для Production, Preview, Development):

```env
VITE_SUPABASE_URL=https://ykdtitukhsvsvnbnskit.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlrZHRpdHVraHN2c3ZuYm5za2l0Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjE2Nzg3MjAsImV4cCI6MjA3NzI1NDcyMH0.tjCfpEG30rxaCuu22EmV3kKGxH45FDMTJNuPknpsl7w
```

**Важно:** После добавления переменных окружения нужно **пересобрать проект**:
- Перейдите в **Deployments**
- Нажмите на последний deployment → **"Redeploy"** → **"Redeploy"**

### Проверка деплоя

После деплоя вы получите URL вида: `https://crm-3-0-xxxxx.vercel.app`

Откройте этот URL в браузере и проверьте, что приложение работает.

---

## Часть 2: Подключение домена burodigital.ru

### Шаг 1: Добавить домен в Vercel

1. Зайдите в Vercel Dashboard → ваш проект → **Settings** → **Domains**

2. Нажмите **"Add Domain"**

3. Введите домен: `burodigital.ru`

4. Нажмите **"Add"**

5. Vercel покажет вам DNS записи, которые нужно добавить. **Скопируйте их!**

### Шаг 2: Настроить DNS записи в reg.ru

1. Зайдите в reg.ru → **Домены** → `burodigital.ru` → **Управление**

2. Нажмите **"Изменить"** рядом с "DNS-серверы и управление зоной"

3. Выберите **"Использовать DNS-серверы reg.ru"** (если еще не выбрано)

4. Перейдите в раздел управления DNS записями

### Шаг 3: Добавить DNS записи

Vercel обычно предоставляет два варианта:

#### Вариант A: CNAME записи (рекомендуется)

Добавьте следующие записи:

**Для корневого домена (@):**
```
Type: CNAME
Name: @
Value: cname.vercel-dns.com
TTL: 3600
```

**Для www поддомена:**
```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
TTL: 3600
```

**Примечание:** Некоторые регистраторы не поддерживают CNAME для корневого домена (@). В этом случае используйте Вариант B.

#### Вариант B: A записи (если CNAME не поддерживается)

Если reg.ru не поддерживает CNAME для @, используйте A записи, которые Vercel предоставит:

```
Type: A
Name: @
Value: [IP адрес от Vercel - обычно 76.76.21.21]
TTL: 3600
```

```
Type: A
Name: @
Value: [Второй IP адрес от Vercel, если предоставлен]
TTL: 3600
```

```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
TTL: 3600
```

**Важно:** Используйте **точные значения**, которые Vercel показывает в Dashboard → Domains после добавления домена!

### Шаг 4: Проверка домена

1. После добавления DNS записей подождите **5-10 минут** для распространения DNS

2. В Vercel Dashboard → **Domains** проверьте статус домена:
   - Сначала будет **"Pending"**
   - После проверки DNS изменится на **"Valid"**

3. Vercel автоматически выдаст SSL сертификат (Let's Encrypt) после проверки DNS

### Шаг 5: Проверка работы

1. Откройте `https://burodigital.ru` в браузере
2. Должен открыться ваш CRM проект
3. Проверьте, что `https://www.burodigital.ru` тоже работает (автоматически редиректит на основной домен)

---

## Часть 3: Настройка автоматических деплоев

После подключения GitHub репозитория:

1. В Vercel Dashboard → **Settings** → **Git**

2. Убедитесь, что:
   - Репозиторий подключен
   - **Production Branch** установлен на `main`
   - **Auto-deploy** включен

3. Теперь каждый push в `main` будет автоматически деплоить проект

---

## Устранение проблем

### Домен не подключается

1. **Проверьте DNS записи**:
   - Они должны **точно совпадать** с теми, что указаны в Vercel Dashboard
   - Используйте онлайн инструменты для проверки DNS: https://dnschecker.org

2. **Подождите до 24 часов** (обычно 5-10 минут) для полного распространения DNS

3. **Проверьте статус в Vercel Dashboard** → Domains

### Деплой не работает

1. **Проверьте логи деплоя**:
   - Vercel Dashboard → **Deployments** → выберите deployment → **Build Logs**

2. **Проверьте переменные окружения**:
   - Убедитесь, что все переменные добавлены
   - После добавления переменных нужно пересобрать проект

3. **Проверьте vercel.json**:
   - Убедитесь, что `buildCommand` и `outputDirectory` указаны правильно

### SSL сертификат не выдается

1. Дождитесь проверки DNS (статус домена должен быть "Valid")
2. Обычно SSL выдается автоматически в течение 1-2 минут после проверки DNS
3. Если не выдается, попробуйте удалить и снова добавить домен

---

## Быстрые команды

```bash
# Деплой в production
vercel --prod

# Просмотр текущих деплоев
vercel ls

# Просмотр логов
vercel logs

# Удаление проекта (осторожно!)
vercel remove
```

---

## Полезные ссылки

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation](https://vercel.com/docs)
- [DNS Checker](https://dnschecker.org)
- [Vercel CLI Documentation](https://vercel.com/docs/cli)

